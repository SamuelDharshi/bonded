# ==========================================================================
# CRE PROJECT SETTINGS FILE
# ==========================================================================
# This workflow does not read from or write to a chain — the RPCs below are
# here so you can extend Step 4 into an on-chain `writeReport` without extra
# setup. See the README's "Customization" section.
staging-settings:
  rpcs:
    - chain-name: ethereum-testnet-sepolia
      url: https://ethereum-sepolia-rpc.publicnode.com
    # CRE's account/key-registry lookups (cre account link-key etc.) query
    # Ethereum mainnet regardless of which chain the workflow itself targets.
    # Read-only lookup RPC — no funds needed, no writes go through this.
    - chain-name: ethereum-mainnet
      url: https://ethereum-rpc.publicnode.com

# ==========================================================================
production-settings:
  rpcs:
    - chain-name: ethereum-testnet-sepolia
      url: https://ethereum-sepolia-rpc.publicnode.com
    - chain-name: ethereum-mainnet
      url: https://ethereum-rpc.publicnode.com
