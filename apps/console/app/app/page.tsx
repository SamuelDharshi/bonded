import { Overview } from '../../components/console/owner/Overview';

export const dynamic = 'force-dynamic';

/** /app — where you are, and the one thing to do next. */
export default function AppOverviewPage() {
  return <Overview />;
}
